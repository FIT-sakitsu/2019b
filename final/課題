final html　では、ボタンを使ってそれぞれの関数が働くようにしている。
全体の中で、cssの要素を組み込み、色やサイズなどの変更をしている。

function question では、条件分岐としてswitch文を使った。
これは授業内容のものと似ている。

function math　では、繰り返しの要素が使われている。
１〜１００までの計算をすることで、それが終わるまで繰り返しの計算が行われるようになっている。

次のおつかい　では、HTMLから別のHTMLに移動する仕組みを使った。
また、HTML内でJavaScriptを使う要素も組み込んだ。
これは、配列を組み込むために導入したものだが、複雑だったので他のクラスの友人に尋ねながら作った。
果物の値段と果物の名前が一致しており、ボタンを押すことによってその名前とそれに当たる値段が表示されるようになる仕組みである。


最後に丸ばつクイズである。
これも、他のクラスの友人に大部分を協力してもらいつつ作成したものである。
これは、配列を使ったクイズだが、

window.onload = function() {
これによって、HTMLの読み込みが完了してから動作するようになっている。
https://qiita.com/cervomansan/items/0873026853686503fc3a

また、正解を１、不正解を２とし、その正答数を数える関数と、前の問題のNOに
プラス１をすることによって問題数が増えるような仕組みにした。

最終的に、合計された正答数をアラートするような関数も作った。
