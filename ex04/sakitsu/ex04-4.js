var m = // 1. mを0にする

var n = Number(prompt('何回試行しますか？')) // 「n回繰り返す」のnを事前取得
for (i = 0; i < n; i++) { // 4. 2〜3をN回繰り返す。
  var x = // 2. 1×1の範囲の任意の点(x,y)に点を打つ。
  var y = // 2. 1×1の範囲の任意の点(x,y)に点を打つ。
  if () { // 3.その点の原点からの距離( SQRT(x2+y2))が1以下なら
    // mに1加える。
  }
}

var pi = // 5. 1/4円の面積はπr2/4で、上記の場合r=1なのでπは4m/Nで求められる
alert(pi) // 計算できた円周率を出力
